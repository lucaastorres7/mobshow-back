# Mobshow Backend

### Executar o projeto
1. ```python -m venv .venv``` -> Executar apenas na primeira vez pra criar o inventario
2. ```source .venv/bin/activate``` -> Habilitar o ambiente virtual
3. ```pip install -r requirements.txt``` -> Instalar todos os requisitos do projeto
